#pragma once

// PLACEHOLDER - Networking not yet implemented (see REGOLE.md §9)

/**
 * Networking module - Multiplayer P2P via WebRTC DataChannel.
 * 
 * TODO: Implement host-authoritative P2P networking with WebRTC signaling,
 *       state replication, and input handling.
 */

class NetworkManager {
public:
    // PLACEHOLDER: stub methods only
    bool Init() { return true; }
    void Shutdown() {}
    void Update(float /* deltaTime */) {}  // Parameter not used in placeholder
};
